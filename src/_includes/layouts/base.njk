<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title }} | Ocean Writeups</title>
    <meta name="description" content="{{ description | default('Research notes and writeups.') }}" />
    <link rel="stylesheet" href="{{ "/assets/styles.css" | url }}" />
    <link rel="stylesheet" href="{{ "/assets/prism.css" | url }}" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css"
    />
  </head>
  <body>
    <div class="page-bg"></div>
    <header class="site-header" aria-hidden="true"></header>
    <main class="container">
      {{ content | safe }}
    </main>
  </body>
</html>
